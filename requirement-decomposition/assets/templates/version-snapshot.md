# {{PROJECT}} 版本快照

> 每次评审定版时追加一节，记录当时全库各文档的版本。用途是回溯：某次评审通过的到底是哪一版内容。

| 项 | 值 |
|------|------|
| 文档版本 | V1.0.0 |
| 更新日期 | {{DATE}} |

## 1. 怎么生成

不要手写。手抄的快照必然与实际漂移，那时它记录的不再是基线，而是某人上次记得更新的时刻。

```bash
python3 scripts/validate_requirements.py ./需求文档 --snapshot 2026-08-09 >> ./需求文档/04-版本快照.md
```

脚本读取各文档头部的「文档版本」行生成。缺这一行的产物不会出现在快照里，跑 `--only C11` 查是哪几份。

## 2. 基线记录

评审定版后把脚本输出追加在下面。既有基线不修改——快照是历史，改了就失去回溯价值。

{{SNAPSHOT_ROWS}}

## 3. 变更记录

| 版本 | 日期 | 变更人 | 变更内容 | 影响文档 |
|------|------|--------|----------|----------|
| V1.0.0 | {{DATE}} | —— | 初始骨架 | —— |
